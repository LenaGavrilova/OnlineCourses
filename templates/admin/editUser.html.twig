{% extends 'base.html.twig' %}
{% block body %}
    {% include 'header.html.twig' %}
    <h1>Редактировать пользователя</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
